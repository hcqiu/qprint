# blueprint的分级：
1. 项目。一般为一篇论文，或者agda标准库里的一个最小文件夹。在blueprint文件夹下，项目文件夹一般放置在子领域的文件夹下。
项目文件夹下有一个索引md文件（一般与项目同名），记录该项目内的blueprint索引，以及该项目依赖的项目。项目文件夹下仍然可能有子文件夹。
2. milestone。一般为论文里的一个subsection（结构良好的学术论文的subsection一般含有一些引理、命题和一个主定理），或者论文里的一个（拥有多个前置定义、引理的）数学对象。在形式化标准库中，它一般为一个.agda或.lean文件。在blueprint文件夹下，milestone是一个单独的md文件。
3. blueprint nodes。每个定义、引理、命题、定理、猜想等，在tex里一般用begin、end环境包裹起来的东西。在blueprint的md文件中，每个node用一级标题表示。
以上分级不出现在Qprint的代码中，仅为一种对用户blueprint项目文件夹分类的建议。

# 知识图谱按颗粒度分3类，可手动切换展示：
1. 节点为blueprint节点（md文件的一级标题）
2. 节点为milestone（md文件），边为它里面对其他md文件的引用（可能引用的是一级标题，不过边要标在两个md文件构成的节点之间。双击节点后进入blueprint节点级别，且属于当前milestone的blueprint节点高亮。）
3. 节点为项目（项目有索引md文件，如果没有的话就最小的子文件夹）。双击节点后进入milestone级别，且属于当前项目的milestone高亮。
# 知识图谱按展示范围分2类，可手动切换展示：
1. 全范围：blueprint文件夹下全部
2. 当前项目
图谱的颗粒度为项目时，应全范围展示；图谱的颗粒度为blueprint节点时，默认按当前项目范围展示。