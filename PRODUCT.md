# Qprint 产品说明

[中文](PRODUCT.md) | [English](PRODUCT.en.md) · [文档目录](docs/index.md)

基线：包版本 `0.1.0`，包含 2026-09-20 的 blueprint 图谱分级扩展。本文说明已实现的产品行为；开发契约见 [spec](docs/spec.md)，后续计划见 [TODO](TODO.md)。

## 目标与用户

Qprint 是以 Markdown 为中心的数学知识库和证明导航工具。它把概念说明、论文片段与形式化声明连接起来，帮助研究者从依赖图进入具体论证，再查看对应代码。

主要使用者是整理数学笔记的研究者、阅读形式化项目的学习者，以及维护论文与代码对应关系的作者。用户继续通过普通文件、Git、Obsidian 或其他编辑器管理资料；应用提供索引、阅读、图谱和 Markdown 编辑。

## 核心概念

| 概念 | 产品含义 |
| --- | --- |
| 工作区 | 一套本地目录，由一个服务实例加载 |
| Blueprint 节点 | Markdown 的一个一级标题及其元数据和正文 |
| Milestone | 图谱中的一个 Markdown 文件，不是新增的磁盘数据类型 |
| 项目 | 图谱根据索引文件和目录归属派生的分组，不等同于整个工作区 |
| 绑定 | 节点到 TeX 标签或 Lean / Agda / Coq 声明的连接 |
| 进度 | 作者填写的 `not_started`、`in_progress`、`complete`，不代表编译验证 |

## 典型使用流程

1. 启动本地工作区，将已有资料放入对应目录，或通过界面/CLI 导入 GitHub 仓库和 arXiv 论文。
2. 在 `blueprint/` 中创建 Markdown，使用一级标题组织节点，用 `qprint` 元数据记录依赖、进度和绑定。
3. 在 TeX 内容前放置 `\bpnode{label}`，填写代码声明及必要的文件和行号。
4. 刷新索引，在目录或搜索中选择节点，联动查看描述、论文片段和代码。
5. 切换图谱颗粒度，从项目下钻到文件，再到具体节点；用范围和筛选缩小阅读目标。
6. 在 Markdown 模式编辑整份文件并保存；出现外部修改冲突时重新载入并人工合并。

新文件、重命名和目录调整目前由外部编辑器完成。导入不会自动编写 blueprint，也不会为 TeX 插入标签。

## 当前功能

| 领域 | 已实现行为 |
| --- | --- |
| 知识组织 | 多节点 Markdown、wikilink、依赖与思想来源、反向引用、诊断 |
| 论文阅读 | TeX 章节目录、标签片段、受限 plasTeX 渲染、本地 KaTeX、原文查看 |
| 代码导航 | 三种语言、多声明绑定、显式行号、启发式定位、HTTPS 来源链接 |
| 图谱 | 节点/文件/项目颗粒度、当前项目/全范围、下钻成员高亮、力导向/分层布局、循环支持 |
| 交互 | 搜索、可折叠侧栏、窄屏布局、节点深链、前后导航与滚动吸附 |
| 编辑 | 整份 Markdown 保存、语法校验、版本冲突检测、保存后重建索引 |
| 导入 | GitHub 公共仓库、arXiv PDF 和源码、来源记录、后台任务状态 |

节点图的 `uses` 和 `inspired_by` 保持语义区别；文件和项目图聚合普通引用，不能把聚合边视为已经证明的逻辑依赖。

## 产品边界

当前面向本机、单用户使用。正常阅读与图谱使用本地资源；导入资料及重新获取前端供应商资源需要网络。PDF 是导入保存的资料，现有阅读视图以 TeX 片段为中心，没有 PDF 页码定位功能。

应用不执行 Lean / Agda / Coq 编译，也没有 LSP、自动证明、AI 双向生成、多用户协作或旧 leanblueprint 转换器。`complete` 由作者声明。TeX 渲染不是完整 LaTeX 排版：跨文件输入、外部图像、复杂宏包和多行宏存在限制，失败时保留原文。

图谱分级不要求迁移文件或添加 Project / Milestone 元数据。外部文件修改需手动刷新；导入任务记录在服务重启后清空。尚无大规模图谱性能承诺。

## 成功标准与依据

一个有效节点应能从目录、搜索或图谱打开，并准确呈现其绑定；缺失或歧义绑定应明确提示。编辑冲突不得静默覆盖；导入不得覆盖既有目标。图谱下钻应显示正确成员和范围，正文选中与成员高亮应分别保持。

这些目标的测试条件定义于 [spec](docs/spec.md)，已执行的自动测试、浏览器操作及网络冒烟记录于 [verification](docs/verification.md)。历史验收结果不代表所有论文、仓库或图谱规模均已验证。

原始需求保留于 [Qprint功能需求.md](Qprint功能需求.md) 和 [blueprint分级指南.md](blueprint分级指南.md)。遇到需求与当前能力差异时，以 spec 明确范围，并在 TODO 跟踪未实现项。
