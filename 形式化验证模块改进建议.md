# Formal Project Resolver
原则：尽量使用语言生态自己的标准文件；只有原生机制表达不了的东西，Qprint 才补充。
“标准声明优先”的 resolver：
Formal Project Resolver
    │
    ├─ 读 lean-toolchain
    ├─ 读 lake-manifest.json
    ├─ 读 *.agda-lib
    ├─ 读 _CoqProject / opam / dune
    └─ 读 Qprint-specific metadata

如果lean项目的版本控制文件缺失，直接报错并自动生成包含时间的错误报告。
对于agda要复杂一些，可能*.agda-lib不够，故需要Resolver通过
1. 原生标准配置
2. Qprint metadata
3. 项目 lock/recipe
4. README heuristic
在agda项目文件夹生成
.qprint-formal.yaml：
toolchain:
  agda: 2.8.0

dependencies:
  cubical:
    revision: d0b9c7b0e9e4f816422c3447d7983b03274dd829

# FormalVerificationAdapter
在原有adapter的基础上增加：
1. 根据项目的版本控制文件，选择toolchains/packages里合适的lean/agda版本。如果没有对应版本，自动下载对应版本到toolchains/packages。
可选：其中，lean可用版本管理工具elan。文件结构举例：
  elan/                        # 忽略 Git：lean版本管理器
  toolchains/                        # 忽略 Git：编译器、运行数据、安装记录
    lean/4.19.0/bin/{lean,lake}.exe
    agda/2.8.0/bin/agda.exe
    agda/2.8.0/data/                  # Agda 内置模块等运行数据
  packages/                          # 忽略 Git：形式化库，与编译器分开
    agda/cubical/<完整commit>/

2. 如果verification出现版本问题，自动生成包含时间的错误报告。

# Formalization version helper skills
当形式化因为版本原因出问题时，用ai agent介入。通过撰写skill（可包含md文件和py脚本）实现以下功能：

何时触发：
1. 找不到版本控制文件，生成了版本错误报告
2. 当其他agent认为验证不通过是因为版本原因导致时

agent可以看到：
1. 错误报告
2. 要验证的项目文件夹
3. toolchains/packages

可以修改/新增：
1. 要验证的项目文件夹的版本控制文件
2. toolchains/packages
处理完成后，agent重新触发resolve和verification。