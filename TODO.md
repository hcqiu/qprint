# Qprint 待办

[中文](TODO.md) | [English](TODO.en.md) · [文档目录](docs/index.md)

基线日期：2026-09-20。勾选项表示当前已有能力；未勾选项是建议的后续工作，没有承诺交付日期。历史验证依据见 [验收记录](docs/verification.md)。

## 已完成基线

- [x] [托管工具链](docs/toolchains.md)：仓库内 Lean/Agda、固定 Cubical、项目声明、resolver/context、安装/列出/删除、light/full ZIP。

- [x] Markdown 多节点、元数据、关系、诊断和示例工作区。
- [x] TeX 标签片段、受限 plasTeX 渲染和本地 KaTeX。
- [x] Lean / Agda / Coq 绑定及代码定位。
- [x] [统一验证适配层](docs/formal-verification.md)：Lean / Agda、独立结果、CLI、显式启用的后台 API、超时与诊断。
- [x] [原生项目解析与版本修复](docs/formal-resolution.md)：模块化 resolver、自动补齐固定版本、带时间的报告、全项目检查与独立 helper skill。
- [x] 阅读、搜索、编辑保存、版本冲突检查和节点深链。
- [x] 图谱三种颗粒度、范围切换、项目归属和两级下钻高亮。
- [x] GitHub / arXiv 导入、来源记录、后台任务和归档保护。
- [x] Python 回归测试、图谱状态测试及示例浏览器验收。

## 优先：可靠性与维护

| 状态 | 工作 | 完成条件 |
| --- | --- | --- |
| [ ] | 持续集成与浏览器回归 | 自动执行 Python/JS 测试；覆盖保存冲突、深链、两级下钻、窄屏和导入失败，不依赖真实外网 |
| [ ] | 降低 TeX 渲染对工作区锁的占用 | 慢片段不阻塞无关读取；刷新后旧渲染结果不会进入新缓存；有并发回归测试 |
| [ ] | 文件监听与增量索引 | 外部保存可更新索引；连续写入合并处理；保留可用选中节点并处理删除和重命名 |
| [ ] | 大图基准与性能预算 | 使用固定的 100 / 1,000 / 10,000 节点样本，记录索引、传输、首帧、布局和交互耗时，再确定支持规模 |
| [ ] | 依赖升级与弃用清理 | 处理现有第三方弃用提示，更新锁文件并通过既有回归，不以隐藏 warning 代替兼容修复 |
| [ ] | 文档维护检查 | 将双语配对、内部链接与 API/示例一致性纳入更新流程 |

## 后续：阅读、组织与导入

| 状态 | 工作 | 完成条件 |
| --- | --- | --- |
| [ ] | 多文件 TeX 与更完整宏支持 | 明确工作区内 include 规则、循环及资源上限；支持多行宏并为不支持语法保留定位和回退 |
| [ ] | 更准确的形式化声明定位 | 引入语言语法支持或可选索引；覆盖多行声明、嵌套作用域、重载和歧义，显式行号仍可优先 |
| [ ] | 文件管理与重命名辅助 | 能创建/重命名 Markdown；提供受影响链接预览和冲突处理，不静默破坏节点引用 |
| [ ] | 图谱状态恢复与分享 | 约定 URL/本地状态格式，恢复层级、范围和焦点；旧链接及已删除对象有回退 |
| [ ] | 可访问性完善 | 检查键盘遍历、焦点恢复和读屏说明；颜色不是表达进度和成员高亮的唯一方式 |
| [ ] | 导入任务持久化与取消 | 重启后能说明任务状态；取消清理暂存且不影响已完成导入；重复提交行为可预测 |
| [ ] | 崩溃恢复及导入配额 | 识别遗留暂存与半完成发布；定义累计磁盘/内存限制，覆盖进程中断后的恢复流程 |
| [ ] | 论文 PDF 导航 | 定义与 TeX 节点的映射来源；无法精确映射时明确显示，而非猜测页码 |

## 需另行设计的扩展

- [ ] 独立 Python 运行时/离线 wheels、更多平台和版本、Coq 安装器、Lean 共享库缓存及任意项目依赖完整打包。

- [ ] 验证扩展：Coq 适配器、Lean 公理/sorry 审计、特殊名称与参数化 Agda 模块、真实工具链 CI、可取消任务、完整依赖指纹和界面结果展示；LSP 另行设计。
- [ ] 旧 leanblueprint 迁移工具：提供只读分析与转换预览，保留原始资料；原始需求中的该项属于后续范围。
- [ ] AI 辅助或双向生成：定义输入上下文、差异预览、用户确认和来源记录，不能自动将生成内容标为已证明。
- [ ] 远程与多用户部署：先设计认证、授权、隔离、并发编辑和持久任务；当前本地 token 不能直接承担这些职责。

## 维护约定

完成任务时同时更新相关中英文模块文档及 spec；只有实际执行过的测试才写入验收记录。发现范围变化时更新 [产品说明](PRODUCT.md) 和 [架构](ARCHITECTURE.md)，避免把路线图描述成现有功能。
